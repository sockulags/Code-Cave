@model PostsVM

@{
    ViewData["Title"] = "Index";
}

<h1>Posts</h1>

<p>
    <a asp-action="AddOrEditPosts" asp-route-id="@null" asp-route-subCategoryId="@Model.SubCategoryId" >Create New Post</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Model.SubCategoryName
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Posts)
        {
            <tr>
                <td>
                    <a asp-action="AddOrEditPosts" asp-route-id="@item.Id">@item.Title</a>
                </td>
                <td>
                    <a asp-action="AddOrEditPosts" asp-route-id="@item.Id" asp-route-subCategoryId="@Model.SubCategoryId" >Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
